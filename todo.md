# TODO

- Turn on dark mode via defaults
- Hide widgets in Stage Manager mode
- Add ABC / Danish keyboard input sources to menu bar
- Add skhd-zig install (jackielii/tap/skhd-zig) once brew tap is confirmed working
